import React from 'react';
import Header from '../components/Header';

const Home = () => (
  <div>
    <Header />
    <main>
      {/* Removed the ResumeSection component */}
    </main>
  </div>
);

export default Home;
